# Test and lint tools, pinned exactly. Also installs the runtime deps.
#
#     pip install -r requirements-dev.txt
#
# The GitHub Action (.github/workflows/ci.yml) installs exactly this too,
# so that local and CI run the same versions.

-r requirements.txt

anyio==4.15.1           # tests/test_mcp_server.py drives the async MCP API
coverage==7.16.1
pillow==12.3.0          # tests/test_version.py stamps the DMG picture (packaging/dmg/stempel.py)
pytest==9.1.1
pyyaml==6.0.3           # tests/test_openapi.py reads openapi.yaml; the app does not
ruff==0.16.8
