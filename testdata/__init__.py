"""The synthetic archive: a whole profile with all eight sources, generated.

`python3 -m testdata.build --profile testdaten` writes it; testdata/build.py
says what it produces and why it is generated rather than committed.
"""
