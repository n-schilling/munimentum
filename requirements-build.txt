# Only for bundling the app (packaging/app.spec) – not needed at runtime.
#
# Pinned for the same reason as requirements.txt: a new PyInstaller major
# likes to change the spec API or the bootloader behaviour, and that would
# only show up at release time. When bumping: build locally and run
# packaging/smoke_test.py, then commit.

-r requirements.txt

pyinstaller==6.22.2

# Lays out the macOS disk image – window, background picture, icon
# positions (packaging/dmg/layout.py). The extra draws the app icon onto
# the volume icon. Only meaningful on macOS, hence the marker.
dmgbuild[badge-icons]==1.6.7; sys_platform == "darwin"

# Writes the version and the build id into the disk image's picture
# (packaging/dmg/stempel.py); the committed PNGs stay the template.
pillow==12.3.0; sys_platform == "darwin"
