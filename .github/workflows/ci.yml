name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        # mcp auf 1.x festgelegt: 2.0 hat mcp.server.fastmcp entfernt
        # (Ersatz: mcp.server.mcpserver). Erst nach Migration von
        # mcp_server.py/rag_server.py anheben.
        run: pip install ruff pytest coverage requests msal numpy "mcp<2"

      - name: Lint (ruff)
        run: ruff check .

      - name: Tests (pytest + coverage)
        run: coverage run -m pytest -v

      - name: Coverage report
        # Erst die Zusammenfassung schreiben, dann das Gate durchsetzen:
        # fail_under steht in pyproject.toml ([tool.coverage.report])
        run: |
          coverage report --format=markdown --fail-under=0 >> "$GITHUB_STEP_SUMMARY"
          coverage report
