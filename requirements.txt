# Runtime dependencies, pinned exactly.
#
# Unpinned, CI has already broken twice because a new major was simply
# picked up (most recently mcp 2.0, which removed mcp.server.fastmcp).
# When bumping: first `pip install -r requirements-dev.txt` + tests locally,
# then commit.
#
# Anyone who only wants to export (outlook_export.py / teams_export.py) needs
# only msal + requests; mcp and numpy belong to the RAG index and MCP server.

mcp==2.0.0              # MCP SDK, 2.x API (MCPServer) – see mcp_server.py
msal==1.37.0            # Graph login (device code flow)
numpy==2.5.2            # embedding matrix for semantic/hybrid search
requests==2.34.2        # Graph REST calls and Ollama


# Native macOS notifications (UNUserNotificationCenter, notify.py). Only the
# bundled app takes this path; run from source it falls back to osascript,
# and other platforms are deliberate no-ops for now.
pyobjc-framework-UserNotifications==12.2.2; sys_platform == "darwin"

