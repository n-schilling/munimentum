# Laufzeit-Abhängigkeiten, exakt gepinnt.
#
# Ungepinnt hat CI schon zweimal gebrochen, weil ein neuer Major einfach
# eingesammelt wurde (zuletzt mcp 2.0, das mcp.server.fastmcp entfernt hat).
# Beim Anheben: erst lokal `pip install -r requirements-dev.txt` + Tests,
# dann committen.
#
# Wer nur exportieren will (outlook_export.py / teams_export.py), braucht
# davon nur msal + requests; mcp und numpy gehören zu RAG-Index und MCP-Server.

mcp==2.0.0              # MCP-SDK, 2.x-API (MCPServer) – siehe mcp_server.py
msal==1.37.0            # Graph-Login (Device-Code-Flow)
numpy==2.5.2            # Embedding-Matrix für semantische/hybride Suche
requests==2.34.2        # Graph-REST-Aufrufe und Ollama

# cryptography kommt nur mittelbar herein (msal -> PyJWT[crypto]) und wird hier
# kaum gebraucht: die App meldet sich nicht an, sie arbeitet mit eingefügtem
# Token. Ab Version 49 gibt es aber keine macOS-x86_64-Räder mehr. pip baut
# dort dann aus dem Quelltext und bindet dynamisch gegen das OpenSSL des
# Build-Rechners; PyInstaller legt eine andere libssl ins Bündel, und die
# fertige App stirbt beim Start mit "Symbol not found: _SSL_get0_group_name".
# 48.0.1 ist die letzte Version mit universal2-Rad – nur für diesen einen Fall
# festgehalten, alle anderen Plattformen bleiben frei.
cryptography==50.0.0; sys_platform == "darwin" and platform_machine == "x86_64"

